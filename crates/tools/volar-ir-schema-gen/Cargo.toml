[package]
name = "volar-ir-schema-gen"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Generator for Volar IR's portable schema artifacts"
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
