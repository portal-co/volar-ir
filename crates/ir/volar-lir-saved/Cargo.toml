[package]
name = "volar-lir-saved"
version = "0.1.0"
edition = "2024"
license.workspace = true
description.workspace = true
repository.workspace = true

[features]
rkyv = ["dep:rkyv", "volar-lir/rkyv"]

[dependencies]
volar-lir = { workspace = true }
rkyv = { workspace = true, optional = true }
